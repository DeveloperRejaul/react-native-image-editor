/** Shared layout constants. */
export const HEADER_HEIGHT = 56;
export const TOOLBAR_HEIGHT = 72;
export const FRAME_SELECTOR_HEIGHT = 104;

/** Canvas aspect ratio (width / height). Square keeps framing predictable. */
export const CANVAS_ASPECT = 1;

/** Output resolution (longest edge) for the exported image. */
export const EXPORT_MAX_EDGE = 2048;

/** Gesture limits. */
export const MIN_SCALE = 0.5;
export const MAX_SCALE = 8;
